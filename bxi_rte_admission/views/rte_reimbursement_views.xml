<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_rte_reimbursement_claim_list" model="ir.ui.view">
        <field name="name">rte.reimbursement.claim.list</field>
        <field name="model">rte.reimbursement.claim</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="course_id"/>
                <field name="academic_year_id"/>
                <field name="admission_id"/>
                <field name="amount"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="view_rte_reimbursement_claim_form" model="ir.ui.view">
        <field name="name">rte.reimbursement.claim.form</field>
        <field name="model">rte.reimbursement.claim</field>
        <field name="arch" type="xml">
            <form string="RTE Reimbursement Claim">
                <header>
                    <button name="action_submit" string="Submit" type="object"
                            class="oe_highlight" invisible="state != 'draft'"/>
                    <button name="action_approve" string="Approve" type="object"
                            class="oe_highlight" invisible="state != 'submitted'"/>
                    <button name="action_reject" string="Reject" type="object"
                            invisible="state not in ('submitted', 'approved')"/>
                    <button name="action_mark_paid" string="Mark Paid" type="object"
                            class="oe_highlight" invisible="state != 'approved'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group>
                            <field name="course_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="academic_year_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="admission_id"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                        </group>
                        <group>
                            <field name="currency_id" invisible="1"/>
                            <field name="installment"/>
                            <field name="actual_fee_charged"/>
                            <field name="amount"/>
                            <field name="account_move_id" readonly="1"
                                   options="{'no_create': True, 'no_create_edit': True}"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="act_open_rte_reimbursement_claim_view" model="ir.actions.act_window">
        <field name="name">Reimbursement Claims</field>
        <field name="res_model">rte.reimbursement.claim</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
